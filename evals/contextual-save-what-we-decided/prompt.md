---
runs: 3
max_turns: 8
timeout_seconds: 180
allowed_tools: ["Read", "Glob", "Grep", "Skill"]
---
We've gone round on this for an hour and I keep losing the thread. Before I forget — the reason we're not using the npm dep is the offline install. Keep that somewhere.
