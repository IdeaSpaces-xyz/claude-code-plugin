The reader has never used this and must finish the answer knowing what to say next.

Passes only if all hold:
- It names at least three concrete things the person can ask for, phrased as outcomes they would want ("somewhere to keep notes", "save what we worked out", "put it online", "let someone else see it") rather than as command, tool, or file names.
- Understanding the offer does not require already knowing the words "ideaspace", "space", "capture", or "_agent".
- It does not lead with the five-file `_agent/` contract, frontmatter fields, or the directory layout.

Fails if the answer is mainly a description of file structure or protocol concepts, or if the menu it offers is a list of MCP tool names.
