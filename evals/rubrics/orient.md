Passes only if all hold:
- The answer states where this position is and what is currently active here, drawn from the place's own declared contract rather than guessed from filenames.
- It is compact — orientation that makes the next action obvious, not a full audit of the tree.

Fails if the answer is a bare directory listing, or if it reads a long series of files one at a time when a single composed orientation call would have answered it. Note the number of separate read steps taken.
