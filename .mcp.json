{
  "mcpServers": {
    "ideaspaces": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/index.js"],
      "env": {
        "IS_API_KEY": "${user_config.api_key}",
        "IS_REPO": "${user_config.repo_id}"
      }
    }
  }
}
